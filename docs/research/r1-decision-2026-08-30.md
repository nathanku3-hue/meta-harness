# R1-DECISION — `EXPERT_JOURNEY_MEASURE_1`

Date: 2026-08-30
Status: **ROUND 1 MEASUREMENT CLOSED — R2 ADAPT WARRANTED**
Authority: non-authoritative roadmap/evidence decision; does not change owner-authored `PRODUCT.md` or score
Campaign base: `57a2564f1fe199de3a8ab8cfb8ae45834e9ec656`

## User journey executed

R1 froze five field-expert falsification families and ran today's product unchanged until an observed failure reached the precommitted causal boundary.

```text
field-expert journey
→ frozen kill rules
→ current production
→ observable failure
→ cause attribution
→ deletion/simplification first
→ one bounded ACI probe
→ one smallest correction
```

## Observable result produced

Independent fixture verdicts:

```text
F1 OPEN_EXISTING          FAIL 0/3
F2 PURSUE_CONSTRAINED     FAIL 0/3 at research-promotion membrane
F3 CHANGE_JUDGMENT        DIAGNOSTIC: visible Claim survives objective revision
F4 CLOSE_STREAM           FAIL 0/3 end-to-end; close semantics unmeasured behind same membrane
F5 GREENFIELD             DIAGNOSTIC GAP: requires existing managed repository
```

No composite field-expert score is calculated.

The current browser proof also expired under one-slot sequential discipline, but retained/new evidence classifies that observation as host/bootstrap/callback friction rather than a demonstrated local browser/RAM defect.

## What the user accomplished or learned

R1 falsified the assumption that the next problem is prompt tuning or broad browser substrate selection.

The first active bottleneck is earlier and narrower:

> **Current field-expert evidence cannot reliably reach the planner through the current structured research/model interface.**

Two concrete current-interface defects were observed on the same seam:

1. the content-only research promoter uses a non-Git temp working directory but does not opt into Codex's supported `--skip-git-repo-check` automation mode;
2. after that single diagnostic ablation, the current model API rejects the research structured-output schema because `schemaVersion` uses `const` without an explicit JSON Schema `type`.

The current Codex runtime/auth path itself is healthy: Codex CLI `0.144.1` successfully completed a trivial production-style ephemeral read-only invocation.

F1 separately proves that stable ACP v1 is exact-text-only and does not yet provide field-expert artifact ingress. F5 separately confirms the current supported entry requires an existing managed repository. Those are R3 productization gaps, but they are not the nearest executable correction while the current research/planner model membrane is broken.

## Product blocker

```text
failure class:
agent/artifact legibility / ACI

observed surface:
current structured research-model invocation

impact:
F2 and F4 cannot reach planner semantics;
field-expert artifact/procedure evidence cannot be evaluated end-to-end.
```

Prompt/model-personality conclusions are invalid until this membrane works.

## Deletion-first check

Do not infer “more tools” from the ACI failure.

R2 must first choose the smallest compatibility repair at the existing structured-model boundary:

```text
preferred question:
Can provider-side schema machinery be simplified/deleted
while retaining Meta-Harness's existing local deterministic validators?

if not:
make only the required retained schemas type-complete
for the current API.
```

For the research promoter's intentionally non-Git content-only temp directory, use the existing Codex automation affordance rather than moving the promoter into repository authority or adding a provider layer.

Do not add:

- provider/model framework;
- ACI framework;
- prompt optimizer;
- model personality database;
- browser router;
- remote browser candidate;
- memory subsystem;
- skill subsystem;
- MCP migration;
- semantic reviewer court.

## Smallest warranted production change

### R2 — `STRUCTURED_MODEL_COMPAT_1`

**Product result:** current content-only expert evidence promotion can run through the existing structured-model membrane under the installed Codex/API contract without weakening local output validation or authority boundaries.

Start with the measured research path only:

```text
research promoter temp cwd
→ use existing non-Git automation allowance

research output schema
→ provider-accepted type-complete shape
   OR delete provider-side schema if local validation proves sufficient
```

Then immediately rerun the held-out R1 F2/F4 fixtures.

Expand the schema correction to planner/worker/challenger/delegation surfaces **only when deterministic compatibility inspection or the rerun proves the same API requirement applies**. Do not turn the repair into a provider abstraction.

### Done when

1. the exact current research promotion invocation succeeds on the frozen F2/F4 artifacts;
2. exact source quotation attribution/local schema validation remains intact;
3. no authority, write, Git, publication, or memory boundary weakens;
4. the repair is tested against the current Codex/API contract;
5. F2/F4 are rerun from frozen fixture bytes so semantic behavior becomes measurable;
6. existing unchanged deterministic authority regressions remain green.

### Stop only if

- the current API cannot support the existing structured-result safety contract without materially weakening validation;
- the required correction expands beyond the shared model-output compatibility seam;
- fixing research promotion would require provider architecture, model fallback, global installation mutation, or owner credentials/access.

## R3 retained evidence, not activated yet

After R2 removes the measurement blocker, R1 already provides two R3 warrants to retain:

```text
F1 → artifact/procedure-aware field-expert ingress is absent
F5 → greenfield product-contract/bootstrap journey is absent
```

Basic semantic close remains an R3 target only after F4 is rerun past the ACI blocker. F3 remains R6 diagnostic evidence for continuity-preserving expert judgment changes.

## Browser disposition

No browser production change is warranted.

The new sequential proof expired, consistent with retained host-bootstrap failures, but there is still no measured local RAM/browser resource defect. Therefore the precommitted remote-browser escape hatch does not fire.

## Memory disposition

`CONTINUITY-BENCH-1 = NO_BUILD` remains closed. No new missing durable knowledge class was observed.

## Score disposition

```text
current formal score remains 86/100
```

R1 does not raise the score. F1/F2/F4 failures also prevent a new field-expert capability claim. `88+` and `90+` evidence boundaries remain unchanged.

## Next executable product action

Implement **R2 `STRUCTURED_MODEL_COMPAT_1`** as the smallest measured correction, then rerun the frozen F2/F4 semantic fixtures before any prompt tuning, browser expansion, memory work, or full R3 expertization.
