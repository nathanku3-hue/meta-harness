# Current Task — Close 0.4, Then Start Minimal Outcome 0.5A

## Immediate product result

Complete Meta-Harness 0.4 H3R2 without changing its source or rebuilding its authoritative package.

## Immediate sequence

1. verify the exact H3R2 branch is durably available at commit `96752ecc2ebd05a897efce7cc6ce5aa56b21ffd7`;
2. use the already-built exact tarball;
3. install it into one clean Node 20+ canary;
4. run one fresh-session terminal Product Episode;
5. perform one terminal Product/System/Release assessment;
6. tag, publish, reconcile, and close using the same artifact;
7. run one real-repository canary before wider rollout.

No new H3R2 source change is authorized unless the real Episode demonstrates a concrete product defect.

## Next product slice after closure

Open `0.5A — Minimal control loop and Git autopilot`.

Product claim:

> A fresh session can resume one product outcome, preserve a dirty user checkout, create a clean isolated worktree automatically, execute the real Product Episode, commit and configured-push the coherent result, and stop without a long prompt, routine owner-approval wait, or successor drift.

Required behavior:

- PLAN, WORK, and AUDIT are separate roles;
- PLAN emits one compact Slice Charter;
- WORK executes it and cannot re-plan roadmap or acceptance;
- AUDIT evaluates only the current slice and cannot select a successor;
- dirty user work remains untouched;
- Git root, default branch, upstream, HEAD, dirty state, and worktrees are detected;
- clean worktree and deterministic slice branch are created automatically;
- reversible local work, commit, and configured branch push proceed automatically and are reported;
- external or irreversible actions are announced before execution;
- no automatic PLAN after closure;
- normal output is a Product Result Card.

## Explicit exclusions

- no broad orchestration rewrite in H3R2;
- no multi-agent fan-out;
- no universal context-scoring system;
- no status-driven roadmap;
- no compatibility for retired process concepts;
- no mandatory retrospective or research round;
- no evidence-only, review-only, package-only, integration-only, or documentation-only product slice.
