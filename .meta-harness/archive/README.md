# Meta-Harness Archive

This directory contains tracked, redacted governance archive material for the source repo.

Do not place runtime traces, provider outputs, secrets, local locks, or noisy run artifacts here. Keep those under `.meta-harness/local/` or another ignored runtime path.
