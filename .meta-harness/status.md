# Meta-Harness Current Product State

State: CODING_SYSTEM_1_COMPLETE
Current checkout: `integration/post-0.4`
Accepted product score: `85/100`

## Product result

A solo developer states one accepted coding result once. Meta-Harness carries that result into an actual coding worker, preserves it through validation and repair, handles dirty worktrees without destroying owner work, and returns the observable product result before internal evidence.

## Current state

- The prevention-pilot stream is complete and is not the roadmap.
- Quant operation is explicitly removed from the Meta-Harness roadmap.
- `work-session/v1` now binds product result, journey state, immediate action, newly true behavior, done definition, stop conditions, reversible authority, owner-only actions, allowed paths, dirty policy, validation, and retry budget.
- `meta-harness work` is the only public command and the default help surface.
- Existing custody, governance, review, release, and maintenance commands remain internal advanced tools; no compatibility aliases are added.
- The coding loop can continue coherent in-scope dirtiness, isolate unrelated dirtiness in a sibling worktree, launch Codex read-only, validate and materialize bounded file contents through the controller, run exact validation, retry bounded failures, and reject path, index, branch, or HEAD escape.
- Post-phase reflection guidance is installed into active repository instructions and synchronized from one canonical template.
- A fresh real-Codex coding journey completed through controller-owned materialization and exact validation.
- Meta-Harness self-hosted one bounded test addition without a planning restart or routine owner intervention.
- Complete verification passes: 110 test files, 756 tests, zero failures; sync checks 33 installed artifacts; whitespace and package closure pass.
- The global quality ratchet still reports inherited repository-wide baseline debt. The new coding modules and tests remain within their line budgets, and the public CLI surface is exactly one command. This unrelated debt is retained as non-blocking residue rather than becoming the coding roadmap.

## Do now

No active coding slice. Use `meta-harness work` and wait for observed real-use friction.

## Done when

- default help exposes only the product-facing `work` journey;
- one sealed work session survives planning-to-working handoff and resume;
- clean, coherent-dirty, and unrelated-dirty repositories select the correct workspace behavior;
- a real read-only coding worker returns allowed file contents, the controller materializes them, and exact validation passes;
- validation failure returns to the same session for bounded repair;
- Git and path escapes fail closed;
- the complete repository suite and packaging checks pass;
- the external reflection records the durable roadmap correction;
- no queue, daemon, swarm, RunSpec migration, compatibility layer, auto-commit, auto-push, or auto-publication is introduced.

## Stop only if

The supported coding worker cannot run, the repository cannot be isolated without risking unique work, an accepted path boundary is insufficient, protected access is required, or evidence proves the requested product result materially unsafe or impossible.
