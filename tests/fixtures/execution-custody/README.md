# Execution Custody Test Fixtures

Deterministic local agent and child-process fixtures for host-neutral runtime tests. These fixtures are test-only and are never selected by the production child adapter.
