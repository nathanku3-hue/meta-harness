#!/usr/bin/env node
"use strict";

/** Identity-only native executable fixture; never invoked by the launcher path. */
process.stdout.write("execution-custody-test-native-stub\n");
process.exitCode = 0;
