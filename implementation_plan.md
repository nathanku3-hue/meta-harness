# PRODUCT_DIRECTION_CONTINUITY_1 — Implementation Plan

## Intent

Make owner product direction a pinned, replayable input to every coding round. Session continuity already exists; product-direction continuity is the active product gap.

## Functional sequence

1. **Bank Episode 1 first** so the new slice starts from a clean tip (`58a8b2a`).
2. **Owner PRODUCT.md** at repository root with required headings; never generated by init or workers.
3. **work-session/v2** replaces v1 with exact `productDirection` content + raw-byte digest; drop thin intent mirror.
4. **Pin before work** for `--goal`, `--session`, and `--resume`; live file must exist and match.
5. **Worker prompt order**: direction → result → journey → do now → done when → repo instructions → implementation context.
6. **Controller guarantee**: reject any materialization targeting `PRODUCT.md` regardless of allowedPaths.
7. **Stop new phase-map install** on init; tolerate absence in retained readers.
8. **Prove with journey tests**, not style guidance. Keep score at 84 until real external revised-85 journey.

## Exact product files

```text
PRODUCT.md
lib/product-direction.js
lib/work-session.js
lib/coding-worker.js
lib/work-materializer.js
lib/commands/work.js
lib/commands/init.js
lib/work-git.js
lib/work-loop.js
templates/contracts/work-session-v2.md
docs/product/product-spec.md
docs/product/roadmap.md
README.md
AGENTS.md
tests/product-direction-continuity.test.js
tests/helpers/product-direction.js
```

## Forbidden expansion

No ProductState database, taste mining, candidate registry, dashboard, daily alignment cron, multi-system governance rewrite, or v1 compatibility aliases.
