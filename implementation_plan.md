# Active Implementation Plan: D085 R0–R6 External Product Proof

Status: **active under D086; R0 and R1 done; R2 human target lock is current; R3 execution unauthorized until R2 exits.**

```text
ROADMAP_PROOF_SCORE = 30 / 100
S-006M_EXTERNAL_LOOPS_SHIPPED = 0 / 1
```

## Endgame intent

Use a minimal authority-bound execution harness to help one solo developer/researcher combine coding and specialist knowledge into merged and packaged external product outcomes. Preserve `intent-v1` verbatim; D085 changes roadmap proof sequencing, not product intent.

## Active proof boundary

S-006M uses Option A:

- prove one external workflow where explicit specialist knowledge materially shapes the result;
- independently verify coding, scope, custody, tests, and observable behavior;
- do not claim independent domain correctness, generalization, or real-world value;
- carry that limitation in the merged and packaged artifact itself;
- test the domain axis independently in R6.

`MERGED + PACKAGED` closes R4. `PUBLISHED`, deployed, live-capital, broker, credential, and public-performance states require a separate named human gate.

## R0 — Shipped substrate

**DONE — score 20/100.**

- `origin/main` and `v0.3.0` resolve to `0b4e0dd7df766a701086d6b611556d9f321a0cb7` in reconciled remote evidence.
- S-001 candidate `588bbe9` accepted.
- verifier-only epoch 2 active;
- package `@nkgss/meta-harness@0.3.0`;
- epoch-1 evidence frozen.

## R1 — Clean target-independent Meta-Harness execution base

**DONE — independently accepted under D086; score 30/100.**

1. Reconcile live and local `origin/main` plus peeled `v0.3.0` at `0b4e0dd7df766a701086d6b611556d9f321a0cb7`.
2. Bind one clean accepted Meta-Harness source worktree to commit `0b4e0dd7df766a701086d6b611556d9f321a0cb7` and tree `29e0d4aaf0235f84dea8d958e5dd8500f16f47f0`.
3. Pack and install exact `@nkgss/meta-harness@0.3.0`; record tarball and installed identity.
4. Bind native Windows Node `25.2.1` at `D:\nodejs\node.exe` and npm `11.16.0` at `D:\nodejs\npm.cmd`; stop below Node 20.
5. Record verifier-only authority, allowed reversible actions, forbidden commitments, and stop rules.
6. Preserve dirty or stale operator checkouts without promoting them to execution authority.
7. Keep the external repository, target commit, target worktree, product behavior, alternatives, and RunSpec unset for R2.

## R2 — Representative external target and slice selection

**CURRENT — human target lock required; score after exit 40/100.**

R2 owns the external repository, exact base commit, clean target worktree, product behavior, alternatives, and RunSpec. Human target lock:

```text
User:
Job or progress sought:
Specialist judgment generic coding cannot replace:
Observable result after shipment:
```

The selected slice must:

- name a real non-Meta-Harness product repository and exact base commit;
- create one clean isolated target worktree;
- trace explicitly to `docs/product/product-intent.md` and `intent-v1`;
- be the smallest representative coding-plus-knowledge behavior, not the smallest trivial merge;
- compare three materially different alternatives;
- state recommendation, strongest counterargument, claim ceiling, cheapest alternative, and reversing evidence;
- bind exact installed `0.3.0`, authority, owned/forbidden surfaces, budget, rollback, and stop rules;
- emit a bounded RunSpec;
- target `MERGED + PACKAGED` without requiring publication.

A provisional theme such as G08 is not selected until these fields are concrete.

## R3 — Execute and independently accept S-006M

**BLOCKED ON R2 — score after exit 60/100.**

```text
frozen audit
→ verbatim original intent
→ three alternatives
→ bounded RunSpec
→ installed 0.3.0 execution custody
→ observable product result
→ independent acceptance
```

The candidate or its durable user-facing documentation must include:

> Specialist knowledge materially shaped this result. Independent domain correctness, generalization, and real-world value remain unproven.

If the same R3 execution exposes a concrete Meta-Harness blocker, remain inside R3:

```text
name exact blocker and failed evidence
→ implement only the smallest proven harness repair
→ prove the repair against that blocker
→ resume the same external slice
→ independent acceptance
```

Do not select a replacement product, broaden scope, or award partial points. Only an independently accepted external product candidate earns R3 points. Rejected or incomplete work may be retained under the normal salvage classifications but does not raise the score.

## R4 — Merge and package

**BLOCKED ON R3 — score after exit 80/100; S-006M 1/1.**

1. Integrate against the current target branch.
2. Rerun product and package acceptance checks.
3. Merge the external behavior.
4. Produce and verify a reproducible distributable artifact.
5. Preserve the claim ceiling in the artifact.
6. Record base/merged commit, package identity, rollback, elapsed time, interventions, rework, and unresolved limits.

Stop before publication unless a separate human gate authorizes it.

## R5 — Learning and validated handover

**BLOCKED ON R4 — score after exit 90/100.**

Produce one evidence-bound learning record and one canonical continuation state. A fresh receiver must restate objective, current state, completed work, unresolved work, exact next action, forbidden action, and done condition.

Do not promote one case beyond verified observation or tested hypothesis without additional evidence.

## R6 — Independent domain-axis validation or replication

**BLOCKED ON R5 — score after exit 100/100.**

Use a named independent domain reviewer, authoritative benchmark, blinded comparison, pre-registered rubric, or equivalent falsifiable method to test the specialist judgment and the transferability of R5 learning against the already merged and packaged S-006M result.

R6 contains no shipment-repair path. Any pre-acceptance harness blocker belongs to the bounded R3 exception loop.

## Held work

No generic guided-pilot platform, research-agent platform, knowledge database, provider expansion, broad handoff framework, outcome-learning engine, generic loop controller, mass module splitting, or multi-agent orchestration may proceed in parallel.

## Stop rule

Do not edit `.meta-harness/status.md` manually. Complete the four-line human target lock before selecting an R2 repository, and stop before R3 implementation until the full R2 exit contract passes.

Also stop on private material leakage, dual-epoch runtime support, silent intent or claim drift, a trivial coding-only target, missing artifact-level claim boundary, publication being conflated with packaging, reopening S-001 architecture without a concrete R3 blocker, or claiming domain correctness from S-006M.


# Historical Plan: Phase 13A Context Quality Gate

> Retained as implementation evidence only. It does not define current product direction after D080/S-001R3.

## Phase 13B Status Note

As of 2026-06-12, D034 authorizes a bounded Phase 13B dogfood/readiness hardening pass after the patched plan. The local implementation fixes placeholder status evidence handling, adds real `context --target` routing for check/ask/packet, expands deterministic verdict fixtures, proves the excellent fixture with file evidence rather than hints, adds ready read-only filesystem snapshot coverage, and reruns dogfood with explicit ROUND-011/012/013 artifacts.

## D033 Status Update

As of 2026-06-12, D033 records the bounded local Phase 13A implementation authorized after the user request to patch the plan and implement via subagents. The D032 planning-only language below is retained as historical planning context; it no longer means the implemented context CLI/core helpers/templates/tests/ready/security wiring are pending.

## Ground Truth Assessment (historical D032 baseline)

**Repo state verified at 2026-06-12 against shipped artifacts:**

- Phase 1–12 closed under D031 at commit d031c
- 24 CLI commands in [command-registry.js](lib/command-registry.js)
- 10 shipped skill templates in [templates/skills/](templates/skills)
- 13 contract templates in [templates/contracts/](templates/contracts)
- 1 prototype skill (`repo-adoption-doctor`) in [skill-registry.json](.meta-harness/skill-registry.json), `active_skills: 0`
- 47 library modules under [lib/](lib), 39 test files under [tests/](tests)
- Existing gates: `boundary-gate`, `scope-selector`, `expert-context-packer`, `ship-fast-decision-router` — all template skills, not CLI commands
- No `context` command exists. No context schema, no context packet command, no context-quality-gate skill
- [status.md](.meta-harness/status.md): "Next action: None. Conclude the aggregate roadmap closure"
- [phase-map.md](.meta-harness/phase-map.md): `intake → plan → work → verify → synthesize → handoff → lookback`
- [roadmap.md](docs/product/roadmap.md): Phase 13 = multi-repo rollup, Phase 14 = controlled autonomy
- `.meta-harness/local/` is gitignored in [.gitignore](.gitignore) (currently line 71)
- `scanRedactionSurfaces` in [redaction-check.js](lib/redaction-check.js) (currently lines 135–142) uses hardcoded default surface list, accepts optional `{ surfaces }` parameter
- [security-check.js](lib/security-check.js) (currently line 206) calls `scanRedactionSurfaces({ targetRoot: root })` without passing policy surfaces
- [security-policy.json](.meta-harness/security-policy.json) (currently lines 29–34) declares `redaction_surfaces` but no code reads it at scan time
- [check-id-registry.js](lib/check-id-registry.js) has 18 check IDs in sorted `READY_CHECK_IDS` array
- [templates install](lib/commands/templates.js) requires `--allow-dirty` when repo has uncommitted changes

**Agreement rating:**

| Dimension | Score |
|-----------|-------|
| Architecture alignment with repo direction | 9.3/10 |
| Implementation alignment with shipped surface | 5.8/10 |

The concept is highly aligned with Meta-Harness philosophy, but zero context-gate artifacts exist today.

---

## Why Phase 13A Before Multi-Repo Rollup (Phase 13)

The next system bottleneck is not more agents, more roles, or more context volume. It is **deciding whether each round has enough compact, fresh, evidence-backed context to proceed safely.** Silent orchestration needs "correct next step" and "planning sufficient?" gates before larger coordination features.

Phase 13A is a precursor phase. D032 authorized planning docs only. D033 now records the bounded local implementation. A later decision may promote 13A to Phase 13 and renumber rollup → Phase 14, autonomy → Phase 15. Until then, existing roadmap numbering is preserved.

### Key Design Rule

```text
A round may proceed only when the harness can produce a compact, evidence-backed
context packet that a fresh worker could execute without inventing product intent,
stack, scope, proof, or stop rules.

Workers do not decide whether context is sufficient. Meta-Harness does.
```

---

## Step 0: Decision Record

> [!IMPORTANT]
> D032 authorized planning docs and design artifacts only.
> D033 now records the bounded local implementation.

### [MODIFY] [decision-log.md](docs/product/decision-log.md)

Append:

```markdown
## D032: Phase 13A Context-Quality-Gate Planning Authorization

Decision:

Authorize Phase 13A planning docs and design artifacts for the context-quality-gate capability. This does NOT authorize implementation, CLI changes, test changes, or source code modifications.

Rationale:

Phase 1–12 closed under D031. The next system bottleneck is not more agents or context volume but deciding whether each round has enough compact, evidence-backed context to proceed without guessing. Context quality gating should precede multi-repo rollup (Phase 13) because silent orchestration needs "correct next step" and "planning sufficient?" gates before larger coordination features.

Scope:

- Phase 13A planning docs, design artifacts, and schema drafts only.
- Governance/status updates required to record planning state: append one redacted `.meta-harness/events.jsonl` planning event and update `.meta-harness/status.md`.
- Phase 13 (multi-repo rollup) and Phase 14 (controlled autonomy) numbering unchanged until a separate renumber decision.
- Implementation requires a separate D033 authorization after planning review.

Non-Goals:
- No CLI changes, lib/ changes, test changes, or template additions.
- No renumbering of existing Phase 13/14 roadmap entries.
- No implementation authorization.
```

---

## D033 Implementation Design (Not Authorized by D032)

> [!WARNING]
> Everything below this line describes D033 implementation scope.
> It is included here as a design reference for planning review.
> D033 is now authorized; this section remains the bounded implementation design reference.

---

### Step 1: Context Gate Schema

#### [NEW] `templates/contracts/context-gate-schema.json`

Canonical schema lives in packaged templates (source of truth), installed to `.meta-harness/templates/contracts/context-gate-schema.json` by `templates install`. Gate outputs reference but do not embed it.

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "round_id", "generated_at", "transition", "overall_score",
    "verdict", "scores", "correct_next_step"
  ],
  "properties": {
    "round_id": {
      "type": "string",
      "pattern": "^ROUND-[0-9]{3,}$"
    },
    "generated_at": {
      "type": "string",
      "format": "date-time"
    },
    "transition": {
      "type": "string",
      "enum": [
        "intake->plan", "plan->work", "work->verify",
        "verify->synthesize", "synthesize->handoff", "handoff->lookback"
      ]
    },
    "overall_score": {
      "type": "integer",
      "minimum": 1,
      "maximum": 10
    },
    "verdict": {
      "type": "string",
      "enum": ["blocked", "narrowed", "proceed", "excellent"]
    },
    "structural_hard_blockers": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Blockers that no hint or override can clear."
    },
    "evidence_gap_dimensions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Dimensions where a provenance-backed hint could satisfy the gap."
    },
    "unknown_dimensions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Dimensions where file evidence was insufficient. Scored as 1."
    },
    "scores": {
      "type": "object",
      "required": [
        "product_outcome", "scope_boundary", "repo_and_stack",
        "owned_surface", "evidence_plan", "risk_and_stop_rules",
        "freshness", "handoff_completeness"
      ],
      "additionalProperties": false,
      "properties": {
        "product_outcome": { "type": "integer", "minimum": 1, "maximum": 10 },
        "scope_boundary": { "type": "integer", "minimum": 1, "maximum": 10 },
        "repo_and_stack": { "type": "integer", "minimum": 1, "maximum": 10 },
        "owned_surface": { "type": "integer", "minimum": 1, "maximum": 10 },
        "evidence_plan": { "type": "integer", "minimum": 1, "maximum": 10 },
        "risk_and_stop_rules": { "type": "integer", "minimum": 1, "maximum": 10 },
        "freshness": { "type": "integer", "minimum": 1, "maximum": 10 },
        "handoff_completeness": { "type": "integer", "minimum": 1, "maximum": 10 }
      }
    },
    "correct_next_step": { "type": "string" },
    "questions": {
      "type": "array",
      "items": { "type": "string" },
      "maxItems": 3
    },
    "hints_applied": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["dimension", "from", "to", "reason", "author", "expires_at"],
        "properties": {
          "dimension": { "type": "string" },
          "from": { "type": "integer" },
          "to": { "type": "integer" },
          "reason": { "type": "string" },
          "author": { "type": "string" },
          "expires_at": { "type": "string", "format": "date-time" }
        }
      }
    }
  }
}
```

**Rules:**
- `overall_score` is always an integer. When any `unknown_dimensions` remain unresolved after hints, `overall_score = 1`.
- The `scores` object always contains integers — unknown dimensions get `score: 1` and appear in `unknown_dimensions[]`.
- `structural_hard_blockers` cannot be cleared by hints. `evidence_gap_dimensions` can be satisfied by valid hints before verdict calculation.

---

### Step 2: Context Quality Gate Skill Template

#### [NEW] `templates/skills/context-quality-gate.md`

Installed to `.meta-harness/templates/skills/context-quality-gate.md`.

```markdown
---
name: context-quality-gate
description: Score whether a phase transition has sufficient context to proceed without guessing.
---

# Context Quality Gate

Run before every phase transition:
- intake → plan
- plan → work
- work → verify
- verify → synthesize
- synthesize → handoff
- handoff → lookback

## Scoring Dimensions (1–10 each)

1. **Product outcome** — Why now? For whom? What feels done?
2. **Scope boundary** — Smallest useful delivery. Explicit out-of-scope. Forbidden scope.
3. **Repo and stack** — Target repo, stack/framework/runtime, branch/package/app surface.
4. **Owned surface** — Files/directories allowed. Files/directories forbidden. Integration points.
5. **Evidence plan** — Test command. Demo path. Acceptance check. Required citations/proof.
6. **Risk and stop rules** — Secrets/data/provider boundaries. Rollback condition. When to pause.
7. **Freshness** — Are docs/API versions current? Is external context stale or missing?
8. **Handoff completeness** — Worker type. Expected output. Required report fields.

## Verdict Logic

- If any `structural_hard_blockers` exist → `blocked` (no hint can clear)
- If any `unknown_dimensions` remain after applying valid hints → `blocked`
  (unknown dimensions score as 1 in the scores object, overall_score = 1)
- If any `evidence_gap_dimensions` remain after applying valid hints → `blocked`
- Else if any dimension < 4 → `blocked`
- Else if overall < 6 → `blocked` (planning/context insufficient)
- Else if overall 6–7 → `narrowed` (continue only with narrowed scope)
- Else if overall 8–9 → `proceed`
- Else if overall 10 → `excellent`

Averages hide danger. Do not average away a 2.

### Unknown vs. low score

- `unknown` = gate cannot determine quality from files alone. Dimension
  appears in `unknown_dimensions`, scores as 1, and emits a question.
- Low score (1–3) = gate found evidence but it is weak or incomplete.

File presence proves "has a field" but not "field is semantically adequate."
When deterministic checks cannot distinguish template-complete from
evidence-backed, classify as unknown (not low-score).

### Blocker classification

**Structural hard blockers** (hints cannot clear):
- Missing repo target
- Missing stack/framework identity
- Requires secrets/provider access without permission
- Untrusted issue/PR text copied into worker prompt
- Missing out-of-scope boundary for execution transitions

**Evidence gap dimensions** (valid hints may satisfy):
- Freshness uncertain (hint: "confirmed docs current via Context7")
- Acceptance taste unclear (hint: "confirmed MVP criteria with product owner")
- Proof command unverified (hint: "confirmed test command runs locally")
- Handoff format unspecified (hint: "confirmed report template with reviewer")

## Output

Emit to `.meta-harness/local/context/ROUND-NNN.json` by default (gitignored).
Emit to `.meta-harness/local/context/ROUND-NNN.md` as human-readable summary.
Use `--out <path>` to write elsewhere, or `--commit-artifact` to write to
`.meta-harness/context/` (tracked, for intentional archival only).
Reject `--out` paths under `.meta-harness/context/` unless
`--commit-artifact` is present; tracked context artifacts must pass
committed-artifact redaction before write.
Ask at most 3 blocker-clearing questions.
```

---

### Step 3: Context Packet Template

#### [NEW] `templates/skills/context-packet.md`

Installed to `.meta-harness/templates/skills/context-packet.md`.

```markdown
---
name: context-packet
description: Assemble a compact context packet for a fresh worker from harness truth files.
---

# Context Packet

## Sources (read in order)

1. `.meta-harness/status.md`
2. `.meta-harness/phase-map.md`
3. `.meta-harness/events.jsonl` (last 5 events)
4. `.meta-harness/local/context/ROUND-NNN.json` (current gate output)
5. Relevant `.meta-harness/workers/*.md`
6. Relevant `.meta-harness/expert-packets/*`
7. `README.md`, `package.json` / `pyproject.toml` (stack detection)
8. Decision log entries relevant to current scope

## Packet Sections

1. **Goal** — one sentence from gate's product_outcome
2. **Scope** — owned files, forbidden files, out-of-scope
3. **Stack** — runtime, framework, test command
4. **Evidence required** — what proves done
5. **Stop rules** — when to pause instead of improvise
6. **Decisions** — relevant prior decisions by ID
7. **Freshness** — any docs/API uncertainty flags

## Rules

- Max 3 pages equivalent
- No raw chat logs
- No stale truth (gate freshness score must be ≥ 6)
- If gate verdict was "narrowed", packet must state the narrowed scope explicitly
```

---

### Step 4: CLI Command and Core Logic

#### [NEW] `lib/commands/context.js` (~80 lines)

New CLI command module routing three subcommands:

```text
meta-harness context check --from <phase> --to <phase> [--round <id>] [--json]
                            [--out <path>] [--commit-artifact]
meta-harness context packet <round-id> --for <worker|review|planning> [--json]
                             [--out <path>]
meta-harness context ask <round-id> [--json]
```

#### [MODIFY] [command-registry.js](lib/command-registry.js)

Add one entry to `commandSpecs`:

```js
{
  name: "context",
  summary: "Check, assemble, or query round context sufficiency.",
  usage: [
    "meta-harness context check --from <phase> --to <phase> [--round <id>] [--json] [--out <path>] [--commit-artifact]",
    "meta-harness context packet <round-id> --for <worker|review|planning> [--json] [--out <path>]",
    "meta-harness context ask <round-id> [--json]",
  ],
  handler: "./commands/context",
}
```

#### [NEW] `lib/context-gate.js` (~230 lines)

Core gate logic:

- Reads existing harness state files: `status.md`, `phase-map.md`, `events.jsonl`, `workers/`, `expert-packets/`
- Reads repo metadata: `README.md`, `package.json` / `pyproject.toml`
- Scores 8 dimensions from file presence, content markers, and field completeness. Dimensions that cannot be determined from file evidence alone are classified:
  - Into `structural_hard_blockers` if no hint can resolve them
  - Into `evidence_gap_dimensions` if a provenance-backed hint could
  - Into `unknown_dimensions` in either case (scored as 1)
- After hint application (if `hints.json` present), re-evaluates:
  - Satisfied evidence gaps are removed from both lists, score updated
  - Structural blockers remain regardless of hints
  - Remaining unknowns force `overall_score = 1`, `verdict = blocked`
- Computes verdict from dimension scores
- Writes JSON to `.meta-harness/local/context/` by default (gitignored)
- Writes human-readable Markdown summary alongside
- Supports `--out` for custom path, `--commit-artifact` for tracked archival
- Rejects `--out` paths under `.meta-harness/context/` unless
  `--commit-artifact` is present, so tracked context artifacts cannot bypass
  committed-artifact redaction
- No network, no model API, no shell execution
- No runtime dependency additions. Schema validation must be implemented with
  small local validation helpers or direct field checks, not Ajv or another
  JSON-schema package.

#### [NEW] `lib/context-packet.js` (~150 lines)

Packet assembler:

- Reads gate output + harness state files
- Produces compact Markdown packet following the template
- Enforces size budget and freshness requirements

---

### Step 4b: Context Hints (Optional Human Overrides)

#### [NEW] `lib/context-hints.js` (~60 lines)

Reads `.meta-harness/local/context/hints.json` if present.

Schema:

```json
{
  "hints": [
    {
      "dimension": "freshness",
      "value": 9,
      "reason": "Confirmed React 19 docs current via Context7 at 2026-06-12",
      "author": "nathanku3-hue",
      "expires_at": "2026-06-19T00:00:00Z"
    }
  ]
}
```

Rules:

- Hints require `reason`, `author`, and `expires_at` (max 7 days)
- Expired hints are ignored with a warning
- Hints can raise a score and can satisfy `evidence_gap_dimensions`
- Hints CANNOT clear `structural_hard_blockers`
- When a valid hint satisfies an evidence gap, the dimension is removed from `unknown_dimensions` and `evidence_gap_dimensions`, and its score is updated to the hint value
- Hints cannot set a dimension above 9 (10 = excellent requires gate evidence)
- Gate output includes applied hints via a `hints_applied` array:
  ```json
  "hints_applied": [
    { "dimension": "freshness", "from": 1, "to": 9,
      "reason": "...", "author": "...", "expires_at": "..." }
  ]
  ```
- Hint provenance is recorded in gate JSON for auditability

---

### Step 4c: Committed Artifact Redaction

When `--commit-artifact` writes to tracked `.meta-harness/context/`:

- Gate output MUST NOT contain:
  - Raw chat logs or conversation transcripts
  - LLM prompts or completions
  - Secrets, tokens, provider credentials, or API keys
  - Raw issue/PR body text (summarize only)
  - Local filesystem paths outside the repo
  - User-specific environment details

- Gate output MUST pass existing `scanTextForSecrets` before commit. Implementation sketch — actual code must ensure strings are the exact bytes to be written, after redaction-safe summarization and before filesystem write:

#### [MODIFY] `lib/context-gate.js`

```js
if (commitArtifact) {
  const { scanTextForSecrets } = require("./redaction-check");
  // jsonString/mdString must be final output bytes, post-summarization
  const jsonResult = scanTextForSecrets(jsonString, {
    path: `.meta-harness/context/${roundId}.json`
  });
  const mdResult = scanTextForSecrets(mdString, {
    path: `.meta-harness/context/${roundId}.md`
  });
  const allFindings = [...(jsonResult.findings || []), ...(mdResult.findings || [])];
  if (allFindings.length > 0) {
    fail(`Redaction check failed for committed context artifact: ${allFindings.map(f => f.id).join(", ")}`);
  }
  // Only write to tracked path after redaction passes
}
```

- Local outputs under `.meta-harness/local/context/` skip redaction check (they are gitignored and never committed).

---

### Step 4d: Redaction Surface Registration

#### [MODIFY] [security-policy.json](.meta-harness/security-policy.json)

Add `.meta-harness/context/` to `redaction_surfaces` array so committed context artifacts are declared as a policy surface:

```diff
  "redaction_surfaces": [
    ".meta-harness/events.jsonl",
    ".meta-harness/workers/",
    ".meta-harness/expert-packets/",
-   ".meta-harness/briefs/"
+   ".meta-harness/briefs/",
+   ".meta-harness/context/"
  ],
```

> [!WARNING]
> **Policy-only change is insufficient.**
>
> Current `scanRedactionSurfaces()` in [redaction-check.js](lib/redaction-check.js) uses a hardcoded default surface list (currently lines 135–142). [security-check.js](lib/security-check.js) currently calls it as `scanRedactionSurfaces({ targetRoot: root })` without passing policy surfaces. Updating `security-policy.json` alone has no runtime effect.
>
> D033 wires the policy into the call site.

#### [MODIFY] [security-check.js](lib/security-check.js)

Read policy surfaces and pass them to the scanner:

```js
// At the existing scanRedactionSurfaces call (line 206):
const policyPath = path.join(root, ".meta-harness", "security-policy.json");
let surfaces;
try {
  const policy = JSON.parse(fs.readFileSync(policyPath, "utf8"));
  if (Array.isArray(policy.redaction_surfaces)) {
    surfaces = policy.redaction_surfaces;
  }
} catch (_) {
  // Policy missing or malformed — fall through to hardcoded defaults.
}
const result = scanRedactionSurfaces({ targetRoot: root, surfaces });
```

This ensures:
- `meta-harness ready` security check scans committed context artifacts via policy-driven surfaces, not just hardcoded defaults
- Later manual commits to `.meta-harness/context/` are caught by the existing `scanRedactionSurfaces` pipeline
- Local outputs under `.meta-harness/local/context/` are unaffected (gitignored, not a redaction surface)
- Repos without `security-policy.json` or without `redaction_surfaces` fall back to the existing hardcoded defaults (no behavior change)
---

### Step 5: Ready Integration

#### [MODIFY] [ready-check.js](lib/ready-check.js)

Add one new check ID: `MH_CONTEXT_GATE_001`

- Behavior: if `.meta-harness/local/context/` or `.meta-harness/context/` contains any `ROUND-*.json`, validate the selected current artifact:
  - JSON parses successfully
  - Validates against `context-gate-schema.json`
  - Contains required fields (`round_id`, `transition`, `scores`, `verdict`)
  - `generated_at` is not older than 7 days (freshness, not verdict)
- Selection is deterministic: choose the highest numeric `ROUND-NNN` across local and tracked context directories, tie-break by parsed `generated_at`, and fail if the selected JSON is malformed.
- Does NOT check whether verdict is `blocked`, `narrowed`, or `proceed`. A blocked verdict is a correct gate state, not a readiness failure. Verdict enforcement belongs to `meta-harness context check` as a turn-preflight, not to `ready`.
- Repos without context gate artifacts return `{ status: "skip", reason: "no context gate surface", applicable: false }` so strict/release mode does not fail merely because the optional context gate has not been adopted.
- Does NOT require context gate to exist — validates it when present

---

### Step 5b: Check ID Registry

#### [MODIFY] [check-id-registry.js](lib/check-id-registry.js)

Add `"MH_CONTEXT_GATE_001"` to `READY_CHECK_IDS` array (sorted position):

```diff
 const READY_CHECK_IDS = Object.freeze([
   "MH_BRIEF_001",
+  "MH_CONTEXT_GATE_001",
   "MH_CONTRACT_001",
   "MH_DECISION_001",
```

Existing registry tests ([command-registry.test.js](tests/command-registry.test.js) and any check-id stability assertions) must be updated to expect the new ID. Total check count goes from 18 → 19.

---

### Step 6: Template Installation

Add source templates to `templates/`:
- `templates/skills/context-quality-gate.md`
- `templates/skills/context-packet.md`
- `templates/contracts/context-gate-schema.json`

Then run:

```bash
node bin/meta-harness.js templates install --overwrite --allow-dirty
```

This regenerates `.meta-harness/templates/manifest.json` with correct hashes. `--allow-dirty` is required because adding source templates makes the repo dirty before the manifest can be regenerated. Template count goes from 23 → 26. Verify with:

```bash
node bin/meta-harness.js sync check --target .
```

Do NOT manually edit `manifest.json`. Hash drift between source and installed templates will fail sync check.

---

### Step 7: Tests

#### [NEW] `tests/context-gate.test.js` (~200 lines)

Unit tests for `lib/context-gate.js`:

- Score computation from fixture state files
- Hard blocker detection (structural vs evidence gap)
- Unknown dimension handling (scored as 1, appears in `unknown_dimensions[]`)
- Hint application: evidence gap satisfied → dimension removed from unknowns
- Hint rejection: structural blocker remains despite hint
- Hint expiry: expired hints ignored with warning
- Hint ceiling: cannot set dimension above 9
- Verdict logic (`blocked`/`narrowed`/`proceed`/`excellent`)
- `overall_score = 1` when unknowns remain after hints
- Schema validation of output JSON against `context-gate-schema.json`
- Missing file handling (graceful degradation, not crash)

#### [NEW] `tests/cli-context.test.js` (~150 lines)

CLI integration tests:

- `meta-harness context check --from plan --to work --json` → valid JSON with `transition: "plan->work"`
- `meta-harness context packet ROUND-001 --for worker --json` → JSON envelope containing `packet_markdown`
- `meta-harness context ask ROUND-001 --json` → ≤3 questions
- Invalid transition name → error
- Missing round → error
- `--commit-artifact` with redaction failure → refused with error message
- `--out .meta-harness/context/ROUND-001.json` without `--commit-artifact` → refused so tracked context artifacts cannot bypass redaction

#### [NEW] `tests/fixtures/context-gate/`

Fixture directory with:

- Minimal `status.md`, `events.jsonl`, `package.json` for test scenarios
- Pre-built gate outputs for validation tests
- `hints.json` fixtures (valid, expired, structural-blocker-targeted)

#### Redaction surface wiring tests

> [!IMPORTANT]
> D033 includes tests proving the policy-to-scanner wiring works.

Add test cases to existing test files:

**In `tests/redaction-check.test.js`:**

- Fixture where `.meta-harness/context/ROUND-001.md` contains a secret-like string (e.g., `AKIAIOSFODNN7EXAMPLE`)
- Call `scanRedactionSurfaces({ targetRoot, surfaces: [..., ".meta-harness/context/"] })`
- Expected: findings array is non-empty, includes the secret pattern ID

**In `tests/security-check.test.js`:**

- Fixture where `security-policy.json` includes `.meta-harness/context/` in `redaction_surfaces`
- Fixture where `.meta-harness/context/ROUND-001.json` contains a bearer token
- Expected: `SEC_REDACTION_001` (or equivalent security check) fails
- Also test fallback: when `security-policy.json` is absent or has no `redaction_surfaces`, scanner uses hardcoded defaults and does NOT scan `.meta-harness/context/`

#### Existing test updates

- [command-registry.test.js](tests/command-registry.test.js): update expected check ID count from 18 → 19, include `MH_CONTEXT_GATE_001`
- [cli-ready.test.js](tests/cli-ready.test.js): add case for `MH_CONTEXT_GATE_001` shape-only validation (not verdict check), plus strict/release coverage proving absent context artifacts return `applicable: false` and do not fail ready

---

### Step 8: Documentation

#### [MODIFY] [roadmap.md](docs/product/roadmap.md)

Add a Phase 13A planning stub as an interim, non-renumbering subphase immediately before the Phase 13 section. Do not add Phase 13A to the numbered Phase Summary table unless a separate roadmap-renumber decision is recorded; Phase 13 remains multi-repo rollup and Phase 14 remains controlled autonomy.

#### [NEW] `docs/product/phase-13a-context-gate-plan.md`

Design document — this plan, cleaned up for the record.

---

### Step 9: Events and Status Update

#### [MODIFY] [events.jsonl](.meta-harness/events.jsonl)

Append planning event for Phase 13A under decision D032.

#### [MODIFY] [status.md](.meta-harness/status.md)

Update goal, phase, next action to reflect Phase 13A context-quality-gate planning.

---

## What Is NOT In This Phase

1. ❌ MCP integration (later: optional read-only context fetch)
2. ❌ Context7 docs freshness (later: optional adapter)
3. ❌ AGENTS.md / CLAUDE.md export commands (later: `context export --target`)
4. ❌ Multi-repo rollup (Phase 13)
5. ❌ Controlled autonomy (Phase 14)
6. ❌ Agent spawning, dashboard, daemon, queue, sidecar
7. ❌ Model API or network requirement in gate logic
8. ❌ LLM-based scoring (gate is deterministic file-based checks)
9. ❌ Arbitrary shell execution in gate
10. ❌ Decision ledger as structured first-class file (reuses existing `decision-log.md` for now)
11. ❌ Repo-map generation (later: compact summary)
12. ❌ Read-only planning mode flag on `context check` (later: `--planning` vs `--execution`)

---

## File Inventory

### D032 Scope (planning-only authorization)

| Action | Path | Size estimate |
|--------|------|---------------|
| MODIFY | `docs/product/decision-log.md` | +15 lines (D032 entry) |
| NEW | `docs/product/phase-13a-context-gate-plan.md` | ~100 lines |
| MODIFY | `docs/product/roadmap.md` | +20 lines (Phase 13A stub) |
| MODIFY | `.meta-harness/events.jsonl` | +1 line (planning event) |
| MODIFY | `.meta-harness/status.md` | update fields |

**D032 total: ~135 lines across 1 new planning file + 2 docs modifications + 2 governance/status updates.**

### D033 Scope (implementation candidate — not authorized by D032)

| Action | Path | Size estimate |
|--------|------|---------------|
| NEW | `templates/contracts/context-gate-schema.json` | ~60 lines |
| NEW | `templates/skills/context-quality-gate.md` | ~70 lines |
| NEW | `templates/skills/context-packet.md` | ~45 lines |
| NEW | `lib/commands/context.js` | ~80 lines |
| NEW | `lib/context-gate.js` | ~230 lines |
| NEW | `lib/context-packet.js` | ~150 lines |
| NEW | `lib/context-hints.js` | ~60 lines |
| NEW | `tests/context-gate.test.js` | ~200 lines |
| NEW | `tests/cli-context.test.js` | ~150 lines |
| NEW | `tests/fixtures/context-gate/` | ~5 fixture files |
| MODIFY | `lib/command-registry.js` | +10 lines |
| MODIFY | `lib/check-id-registry.js` | +1 line |
| MODIFY | `lib/ready-check.js` | +35 lines |
| MODIFY | `.meta-harness/security-policy.json` | +1 line (redaction surface) |
| MODIFY | `lib/security-check.js` | ~10 lines (wire policy surfaces) |
| REGEN | `.meta-harness/templates/manifest.json` | via `templates install --allow-dirty` |

**D033 total: ~1055 lines across 10 new files + 6 modifications + 1 regen.**

---

## Verification Plan

### Automated Tests (D033)

```bash
# Context gate and CLI tests
node --test tests/context-gate.test.js tests/cli-context.test.js

# Ready check with new MH_CONTEXT_GATE_001 (shape-only, not verdict)
node --test tests/cli-ready.test.js

# Check ID registry count = 19
node --test tests/command-registry.test.js

# Redaction surface wiring
node --test tests/redaction-check.test.js tests/security-check.test.js

# Full suite, no regressions
npm test

# Quality gate still passes
node bin/meta-harness.js quality check

# Templates in sync after install
node bin/meta-harness.js templates install --overwrite --allow-dirty
node bin/meta-harness.js sync check --target .

# Ready still passes
node bin/meta-harness.js ready --target . --quick --read-only --json
```

### Manual Verification (D033)

```bash
# Run context check
node bin/meta-harness.js context check --from plan --to work --json
# Expect: valid JSON with scores (including possible unknowns scored as 1),
#   verdict, blockers, questions
# Expect: .meta-harness/local/context/ROUND-001.json and .md created (gitignored)
# Expect: ready --target . --quick still passes (blocked verdict does NOT fail ready)

# Assemble packet
node bin/meta-harness.js context packet ROUND-001 --for worker
# Expect: compact Markdown packet printed to stdout

# Query blockers
node bin/meta-harness.js context ask ROUND-001
# Expect: ≤3 blocker-clearing questions

# Verify git cleanliness after context check (default output is local)
git status --porcelain=v1
# Expect: no .meta-harness/context/ or .meta-harness/local/ in output
```

---

## Resolved Questions

All open questions resolved through five audit rounds:

1. **Phase numbering** → Phase 13A. D032 authorized planning only, and D033 now records bounded local implementation. A later decision may promote and renumber.
2. **Decision ID** → D032 drafted as planning-only authorization. D033 records implementation authorization.
3. **Gate output location** → `.meta-harness/local/context/` (gitignored). `--commit-artifact` flag for intentional archival only, with redaction check before write.
4. **Human overrides** → Provenance-backed hints with author, reason, expiry (max 7 days). Can satisfy `evidence_gap_dimensions` but cannot clear `structural_hard_blockers`. Cannot set above 9.
5. **Score ambiguity** → `unknown` dimensions score as integer 1, appear in `unknown_dimensions[]`. `overall_score` is always integer; forced to 1 when unknowns remain.
6. **Blocker model** → Split into `structural_hard_blockers` (immutable) and `evidence_gap_dimensions` (hint-satisfiable).
7. **Ready vs verdict** → Ready validates artifact shape/freshness only. Blocked verdict is correct state, not readiness failure.
8. **Template manifest** → Generated by `templates install --allow-dirty`, never manually edited.
9. **Redaction surfaces** → Policy update alone insufficient. D033 wires `security-policy.json` `redaction_surfaces` into `scanRedactionSurfaces` call in `security-check.js`, with tests proving the wiring works.
10. **D032 scope** → Planning docs only. Implementation file inventory was labeled as D033 candidate until the D033 implementation record.
