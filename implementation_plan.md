# CODING_SYSTEM_1 — COMPLETE

## Intent

Make Meta-Harness itself the low-friction coding product. One accepted result must survive the planning-to-working boundary, start coding without another planning climax, handle dirtiness safely, run exact validation, repair bounded failures, and return the observable result before internal evidence.

## Functional sequence

1. **Make `work` the product surface.**
   - Expose exactly one public command.
   - Move all evidence, custody, release, and maintenance commands to advanced help.
   - Render product language first.

2. **Bind the working brief.**
   - Introduce exact `work-session/v1` validation and digest sealing.
   - Support a low-friction `--goal` shorthand and explicit session JSON.
   - Persist the session outside tracked repository bytes for resume.

3. **Resolve dirty work without owner-work mutation.**
   - Continue clean or coherent in-scope work.
   - Preserve unrelated dirtiness and create a sibling worktree.
   - Never reset, clean, stash, revert, stage, or auto-close worktrees.

4. **Run the coding worker.**
   - Launch local Codex read-only with `never` approval policy.
   - Inject product result, state, immediate action, done definition, stop conditions, allowed paths, and owner-only actions.
   - Require complete bounded `{ path, content }` changes rather than direct filesystem mutation.
   - Prohibit planning restart, compatibility work, Git publication actions, deletion, and scope widening.

5. **Materialize and verify outside the model.**
   - Prove the read-only worker left HEAD, branch, index, and working bytes unchanged.
   - Reject traversal, symlink, duplicate, oversized, or out-of-bound file proposals.
   - Materialize validated regular-file writes through the controller.
   - Prove unchanged HEAD, branch, and index after materialization.
   - Run exact commands declared by the work session.
   - Return failed validation to the same session up to the bounded attempt count.

6. **Prove the product.**
   - Deterministic tests cover clean coding, coherent dirtiness, isolated dirtiness, resume, retry, and boundary failure.
   - Run one real Codex coding operation on a disposable repository.
   - Self-host one bounded Meta-Harness change through the same work-session path.
   - Run complete repository tests, sync, whitespace, help, and package verification.
   - Inspect quality separately: require the new coding surface to remain within budget and exactly one command to be public; retain unrelated inherited ratchet debt without converting it into the roadmap.

7. **Retain the durable lesson.**
   - Update `E:\Code\post_phase_reflection.md` with the coding-system roadmap correction.
   - Keep repository-specific execution evidence out of the shared reflection.

## Exact product files

```text
bin/meta-harness.js
lib/command-registry.js
lib/commands/work.js
lib/coding-worker.js
lib/work-git.js
lib/work-loop.js
lib/work-session.js
templates/contracts/work-session-v1.md
README.md
package.json
docs/product/product-spec.md
docs/product/roadmap.md
.meta-harness/status.md
implementation_plan.md
task.md
AGENTS.md
```

The existing active-guidance/reflection files remain part of the same problem-solving correction because they directly solve durable post-phase learning.

## Completion

Complete. `meta-harness work` obtained bounded code from a real read-only worker, materialized it through the controller, and passed exact external validation. Dirty owner work remained untouched, resume preserved the same product brief, Meta-Harness self-hosted a bounded test addition, default help contains no gate-facing journey, 110 test files and 756 tests pass, sync checks 33 artifacts, whitespace and package closure pass, and no compatibility or orchestration framework was added.

## Forbidden expansion

No RunSpec migration, old-runtime restoration, provider abstraction, queue, daemon, swarm, dashboard, auto Git publication, cleanup campaign, or external repository product milestone.
