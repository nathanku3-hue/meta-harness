# Meta-Harness 0.4 H3 Implementation Plan

## Intent

Ship one outcome-first DELIVERY contribution. Governance remains the existing owner pin, signed authorization, one RunSpec, exact changed-path custody, installed proof, isolated reviewers, and terminal closure. No new framework is authorized.

## Functional sequence

1. Correct planner contracts and live template copies.
2. Remove alternate execution authority from public actions, controller capabilities, state, evidence runtime, and package scans.
3. Make worker reports expose the executed journey and observable result before internal metadata.
4. Run focused fixtures:
   - Leningrad complete-journey action and optional finding non-blocking;
   - Quant unaffected-evidence reuse;
   - Eureka terminal `NO_BUILD` / `USE_PRODUCT`;
   - authority-versus-stale-status conflict;
   - valid and invalid continuation warrants;
   - generated worker-report leading fields.
5. Confirm exactly the 46 sealed paths changed.
6. Create one non-merge commit directly on B1R2.
7. Execute the sealed complete-suite mechanics assessment once.
8. Build one authoritative tarball after mechanics passes.
9. Install that tarball into one clean canary and run the fresh-session D14 interaction.
10. Continue terminal proof, exact publication, and closure only with the same tarball.

## Stop law

Do not reopen unchanged passed evidence. Do not add a successor slice, review, evidence gate, or owner request after terminal completion without explicit owner scope change or a complete observed supported-use defect warrant.
